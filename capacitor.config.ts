import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.microbit.ml.prototype',
  appName: 'micro:bit machine learning tool',
  webDir: 'dist'
};

export default config;
