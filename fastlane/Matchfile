git_url ENV.fetch("BRAND_MATCH_GIT_URL", "")

storage_mode "git"

type "appstore"

app_identifier ENV.fetch("BRAND_APP_IDENTIFIER", "org.microbit.mltrainer")

team_id ENV.fetch("BRAND_APPLE_TEAM_ID", "")
