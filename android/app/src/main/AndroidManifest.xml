<?xml version="1.0" encoding="utf-8" ?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <supports-screens
        android:smallScreens="false"
        android:normalScreens="false"
        android:largeScreens="true"
        android:xlargeScreens="true" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <!--
          Disable Firebase's default analytics collection at startup so
          nothing is collected until the user has granted analytics
          consent. NativeLogging.setConsent flips collection back on
          via FirebaseAnalytics.setEnabled() once consent is granted.
        -->
        <meta-data
            android:name="firebase_analytics_collection_enabled"
            android:value="false" />
        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:windowSoftInputMode="adjustNothing"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/octet-stream" />
                <data android:scheme="file" />
                <data android:scheme="content" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW"/>
                <action android:name="android.intent.action.EDIT" />
                <category android:name="android.intent.category.DEFAULT"/>
                <category android:name="android.intent.category.BROWSABLE"/>

                <data android:scheme="content" />
                <data android:scheme="file" />
                <data android:host="*" />
                <data android:pathPattern=".*\\.hex" />
                <data android:pathPattern=".*\\..*\\.hex" />
                <data android:pathPattern=".*\\..*\\..*\\.hex" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.hex" />
            </intent-filter>
        </activity>

        <!--
          App Links for web deep links (e.g. https://<host>/import?...).
          On an activity-alias so it can be toggled per build: the
          deepLinkEnabled / deepLinkHost placeholders (see app/build.gradle)
          are only set to a real host when a brand provides one, so
          unbranded/OSS builds leave this alias disabled and claim no domain.
          autoVerify makes Android verify ownership against
          https://<host>/.well-known/assetlinks.json so verified links open
          the app directly. Targets MainActivity, which handles the intent.
        -->
        <activity-alias
            android:name=".DeepLinkActivity"
            android:targetActivity=".MainActivity"
            android:enabled="${deepLinkEnabled}"
            android:exported="true">
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" android:host="${deepLinkHost}" />
            </intent-filter>
        </activity-alias>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/file_paths" />
        </provider>
    </application>

    <!-- BLE Hardware Features -->
    <!-- Restricts app visibility in Google Play Store to devices with BLE support -->
    <uses-feature android:name="android.hardware.bluetooth_le" android:required="true" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.INTERNET" />

    <!-- Location permissions for BLE scanning on Android < 12 only -->
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" android:maxSdkVersion="30" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" android:maxSdkVersion="30" />

    <!-- BLE permissions for Android 12+ (API 31+) -->
    <!-- neverForLocation flag indicates we don't use BLE scan results for physical location -->
    <uses-permission android:name="android.permission.BLUETOOTH_SCAN"
        android:usesPermissionFlags="neverForLocation"
        tools:targetApi="s" />
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />

    <!-- Foreground service permission for Nordic DFU library -->
    <!-- Note: Android 14+ (API 34+) requires specifying a foreground service type if using foreground services -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />

    <!--
      firebase-analytics injects the advertising-ID and Privacy Sandbox
      ad-services permissions by default. We don't use AdMob, Google
      Signals, or attribution APIs — analytics is gated behind explicit
      consent with AdStorage/AdPersonalization/AdUserData never granted
      (see src/logging/sink.ts). Strip the permissions so the merged
      manifest matches the Play Console "no advertising ID" declaration.
    -->
    <uses-permission android:name="com.google.android.gms.permission.AD_ID" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_ATTRIBUTION" tools:node="remove" />
    <uses-permission android:name="android.permission.ACCESS_ADSERVICES_AD_ID" tools:node="remove" />
</manifest>
